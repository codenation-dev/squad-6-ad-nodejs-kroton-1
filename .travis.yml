language: node_js
node_js:
- '12.10'

install:
- npm i

services:
- postgresql

before_script:
- psql -c 'create database log_api_test;' -U root
- npm run sync:test

env:
  global:
    - PGUSER=root
    - PGPASSWORD=123456789

deploy:
  provider: heroku
  on:
    branch: feature/test-ci