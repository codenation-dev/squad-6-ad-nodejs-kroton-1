language: node_js
node_js:
- '12.10'

install:
- npm i

services:
- postgresql

before_script:
- cp ./src/config/database.yml.travis /config/database.yml
- psql -c 'create database log_api_test;' -U root
- npm run sync:test

deploy:
  provider: heroku
  on:
    branch: feature/test-ci